
export const metadata = {
  title: "Authentication",
  description: "Authentication Page of Demo",
};

export default function RootLayout({ children }) {
  return (

        <div className="">

            {children}


        </div>

    
  );
}